package thirtydaymarathon.week1.day5;

import java.util.Scanner; // Импортируем класс Scanner — для чтения пользовательского ввода из консоли

public class EmailValidator {

    // Главный метод — точка входа. Запускается первым при старте программы
    public static void main(String[] args) {
        Scanner scanner = new Scanner(System.in); // Создаём объект scanner для считывания текста из консоли (System.in)

        System.out.print("Введите email для проверки: "); // Выводим приглашение без переноса строки (cursor остаётся на этой же строке)
        String inputEmail = scanner.nextLine().trim(); // Читаем всю строку, удаляем пробелы по краям (trim)

        // Вызываем наш метод isValidEmail() и передаём введённый email. Результат сохраняем в переменную isValid
        boolean isValid = isValidEmail(inputEmail);

        // Условная проверка: если метод вернул true → email корректен, иначе — нет
        if (isValid) {
            System.out.println("Email корректен."); // Выводим подтверждение
        } else {
            System.out.println("Email некорректен."); // Выводим сообщение об ошибке
        }
    }

    // Метод isValidEmail — логика валидации email
    // Принимает строку (email), возвращает boolean (true/false)
    public static boolean isValidEmail(String email) {
        // Метод вернёт true, если строка содержит символ "@" и точку "."
        return email.contains("@") && email.contains(".");
        // Это простая базовая проверка. В реальности можно использовать регулярные выражения.
    }
}

/*
    ===== День 5: Методы, return, void — Переиспользуемость и чистый код =====

    ✅ Что изучено:

    ➤ Что такое метод:
        ▸ Блок кода, который можно переиспользовать и вызывать по имени
        ▸ Позволяет структурировать программу и разделять логику
        ▸ Методы помогают писать чистый и тестируемый код

    ➤ Сигнатура метода:
        ▸ Это уникальная "внешность" метода, по которой его отличает Java
        ▸ Включает имя метода и типы параметров (без return'а!)
        ▸ Пример сигнатуры: isValidEmail(String)
        ▸ Методы с одинаковыми именами, но разными сигнатурами — это перегрузка

        Примеры разных сигнатур:
        - log(String message)
        - log(String message, int level)
        - log()

    ➤ Структура метода:
        public static boolean isValidEmail(String email)
        ▸ public     — доступен везде
        ▸ static     — не требует создания объекта
        ▸ boolean    — возвращает true/false
        ▸ isValidEmail — имя метода (по смыслу!)
        ▸ (String email) — один параметр типа String

    ➤ return:
        ▸ Завершает выполнение метода
        ▸ Возвращает значение, если тип не void
        ▸ Тип возвращаемого значения должен совпадать с объявлением

    ➤ void:
        ▸ Метод ничего не возвращает
        ▸ Используется, когда метод только что-то делает (печатает, логирует)

    ➤ Параметры:
        ▸ Методы могут принимать 0, 1 или несколько аргументов
        ▸ Возможны массивы, списки, varargs

    ➤ Хорошие имена методов:
        ▸ Глагол + сущность: getUser(), printResult(), isValidEmail()
        ▸ Не сокращай, не пиши method123() — пиши говорящие имена

    ➤ Практика:
        ▸ Метод isValidEmail() с простой валидацией
        ▸ Вызов метода из main() с передачей аргумента
        ▸ Использование return и логических условий

    📌 ИТОГ:
    Методы — фундамент всей архитектуры тестов (PageObject, RestAssured-сервисы, валидации и т.д.)
    Чистая логика всегда выносится в отдельные методы — так легче читать, тестировать и переиспользовать.
*/